---
phase: quick-260801-u8d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - datenschutz/index.html
  - impressum/index.html
  - sitemap.xml
autonomous: true
requirements: [CONSENT-01, DSGVO-02, REVIEW-02, REVIEW-03, REVIEW-04]
tags: [static-site, dsgvo, heizreport, consent, seo, cls]

must_haves:
  truths:
    - "Beim Aufruf der Startseite wird heizreport_3.js NICHT geladen — kein Request an heizreport.de vor einer Nutzeraktion"
    - "In der Sektion #waermepumpencheck sieht der Besucher stattdessen einen Hinweistext mit Link zur Datenschutzerklärung und einen Button"
    - "Nach Klick auf den Button wird das Script injiziert und das Widget erscheint an derselben Stelle"
    - "Das Kundensnippet (Script-URL und die drei data-Attribute) ist inhaltlich unverändert"
    - "Die Datenschutzerklärung nennt Art. 6 Abs. 1 lit. a) DSGVO (Einwilligung) als primäre Rechtsgrundlage für das Widget"
    - "impressum/ und datenschutz/ verwenden dieselbe Containerbreite"
    - "sitemap.xml enthält nur noch die Startseite"
    - "Alle <img>-Tags tragen width/height mit den echten Pixelmaßen der Dateien"
  artifacts:
    - path: "index.html"
      provides: "Zwei-Klick-Consent-Platzhalter, klickgesteuerter Script-Loader, img-Intrinsics"
      contains: "loadWpCheck"
    - path: "datenschutz/index.html"
      provides: "Aktualisierter Heizreport-Abschnitt (Einwilligung), vereinheitlichte Containerbreite"
      contains: "Art. 6 Abs. 1 lit. a)"
    - path: "impressum/index.html"
      provides: "Vereinheitlichte Containerbreite, img-Intrinsics"
    - path: "sitemap.xml"
      provides: "Nur noch indexierbare URLs"
  key_links:
    - from: "index.html Button (onclick={{ loadWpCheck }})"
      to: "this._hrLoad in componentDidMount"
      via: "renderVals-Handler setzt state.wpConsent und ruft den Loader"
      pattern: "loadWpCheck"
    - from: "index.html Platzhalter-Hinweistext"
      to: "datenschutz/"
      via: "Link auf die Datenschutzerklärung"
      pattern: "href=\"datenschutz/\""
---

<objective>
Das Heizreport-Wärmepumpencheck-Widget wird hinter eine Zwei-Klick-Consent-Lösung gestellt: Das externe Script `heizreport_3.js` lädt erst nach aktivem Klick des Besuchers. Zusätzlich werden drei optische/technische Review-Befunde aus dem Vorgänger-Task 260801-tgu geschlossen.

Purpose: Rechtssichere Einbindung des Drittanbieter-Scripts (keine IP-Übertragung an heizreport.de ohne Einwilligung), plus Beseitigung von Inkonsistenzen (Containerbreiten, Sitemap/noindex-Widerspruch, Layout-Shift durch fehlende Bild-Intrinsics).
Output: Angepasste `index.html`, `datenschutz/index.html`, `impressum/index.html`, `sitemap.xml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260801-tgu-site-review-farbiges-logo-auf-allen-seit/260801-tgu-SUMMARY.md

Betroffene Dateien:
@index.html
@datenschutz/index.html
@impressum/index.html
@sitemap.xml
</context>

<constraints>
**Nicht anfassen (verbindlich):**
- `sendMail.php` und das Kontaktformular in `index.html` (Zeile ~1565, `fetch('sendMail.php', …)`) — finales Hosting ist ein PHP-fähiger Server auf ms-bauconsult.de, das Formular funktioniert dort.
- Alle `index_bundled.html`-Dateien — weder ändern noch löschen.
- Das Kundensnippet inhaltlich: Script-URL `https://heizreport.de/js/heizreport_3.js` und `<div id="wp_check_script" class="heizreport" data-heizreport-ansprache="du" data-primary-color="#3371b7" data-heizreport-user="info@ms-bauconsult.de">` bleiben zeichengleich. Nur der **Ladezeitpunkt** ändert sich.

**Sprache/Ton:** Die Seite siezt durchgängig ("Passt eine Wärmepumpe zu **Ihrem** Haus?", "Machen **Sie** den kostenlosen Kurzcheck"). Alle neuen sichtbaren Texte deshalb in Sie-Form, Deutsch mit korrekter Orthographie (ä/ö/ü/ß). Das `data-heizreport-ansprache="du"` des Widgets bleibt davon unberührt (Anbieter-Konfiguration, nicht unser Text).

**Keine Consent-Speicherung:** Kein Cookie, kein localStorage, kein sessionStorage. Pro Seitenaufruf neu klicken ist gewollt.
</constraints>

<interfaces>
<!-- Rendering-Architektur: clientseitig gerendert über assets/bundle_2.js (React-basiert, -->
<!-- Template-Direktiven sc-if / sc-for, Interpolation via {{ }}). Statische <script src>-Tags -->
<!-- im Markup werden vom Renderer NICHT ausgeführt — Drittanbieter-Scripts müssen injiziert werden. -->

Vorhandenes Muster für bedingtes Rendern (index.html:1383–1412) — es gibt KEIN sc-else,
stattdessen zwei sc-if mit einem Flag und seiner Negation aus renderVals():

  <sc-if value="{{ sent }}" hint-placeholder-val="{{ false }}"> … </sc-if>
  <sc-if value="{{ notSent }}" hint-placeholder-val="{{ true }}"> … </sc-if>

  // renderVals(), index.html:1547
  notSent: !this.state.sent,

Vorhandenes Muster für Click-Handler (index.html:1181):

  <button onclick="{{ toggleMenu }}" …>
  // renderVals(): toggleMenu: () => this.setState({ menuOpen: !this.state.menuOpen }),

Aktueller Auto-Loader (index.html:1500, letzte Zeile in componentDidMount) — wird in Task 1 ersetzt:

  var _hrTries=0;var _hrLoad=function(){ … };setTimeout(_hrLoad,0);

Primär-Button-Stil der Seite (index.html:1408, Kontaktformular) als Vorlage:

  height:54px;background:var(--accent,#FFC000);color:#161616;border:none;
  font-family:'Saira',sans-serif;font-size:15px;font-weight:600;letter-spacing:1.5px;
  text-transform:uppercase;transition:background .2s   +   style-hover="background:#A87800"

Sektionsfarben #waermepumpencheck (heller Bereich): background #FFFFFF, Fließtext #6B6864,
Überschrift #181716, Rahmen/Trennlinien #E0DDD8.

Echte Pixelmaße der Assets (per jimp ausgelesen, für Task 3):

  logo.png                                        1281x413
  ms.profil-300x246.png                            300x246
  eee-300x73.png                                   300x73
  shutterstock_34071343.png                        500x375
  shutterstock_61563004.png                       1024x768
  kfw_bafa.png                                    1015x903
  energieausweis-1.jpg                             250x354
  iSFP-sanierung-einzelmassnahmen-bafa-dena.jpg    560x420
  unsplash_01.png                                  500x400
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Zwei-Klick-Consent für das Heizreport-Widget</name>
  <files>index.html</files>
  <action>
Das Widget wird nur noch nach aktivem Klick geladen (locked decision 1). Vier zusammenhängende Änderungen in `index.html`:

**1a — State-Flag ergänzen (state-Objekt, ~Zeile 1458–1469):**
Neues Feld `wpConsent: false,` hinzufügen (z. B. hinter `pastHero: false,`). Kein Auslesen aus Storage — der Wert startet bei jedem Seitenaufruf auf `false`.

**1b — Loader entkoppeln (componentDidMount, Zeile 1500):**
Die bestehende Zeile beginnt mit `var _hrTries=0;var _hrLoad=function(){…};setTimeout(_hrLoad,0);`. Die Loader-Funktion selbst bleibt inhaltlich erhalten (Idempotenz-Guard über `document.getElementById('heizreport-sdk')`, Retry-Schleife mit Abbruch nach 20 Versuchen, Injektion eines `<script id="heizreport-sdk" src="https://heizreport.de/js/heizreport_3.js" async>` an `document.body`). Zwei Änderungen:
  - Der abschließende Selbstaufruf `setTimeout(_hrLoad,0);` entfällt ersatzlos — dadurch lädt nichts mehr automatisch.
  - Die Funktion wird über `this._hrLoad=_hrLoad;` an die Instanz gehängt, damit der Click-Handler sie aufrufen kann. Die interne Retry-Rekursion referenziert weiterhin die lokale Variable `_hrLoad`.

**1c — renderVals() ergänzen (~Zeile 1518 ff., beim Muster von `notSent`/`toggleMenu` einsortieren):**
  - `wpNotLoaded: !this.state.wpConsent,` — steuert die Sichtbarkeit des Platzhalters.
  - `wpDisplay: this.state.wpConsent ? 'block' : 'none',` — steuert die Sichtbarkeit des Widget-Wrappers.
  - `loadWpCheck: () => { this.setState({ wpConsent: true }); setTimeout(this._hrLoad, 0); },` — setzt erst den State (Wrapper wird sichtbar), injiziert das Script danach im nächsten Macrotask, damit der Container beim Initialisieren des Widgets bereits sichtbar ist.

**1d — Markup der Sektion (index.html, aktuell Zeilen 1351–1353):**
Der äußere `<div class="reveal" style="max-width:900px;margin-top:clamp(32px,4vw,48px)">` bleibt unverändert. Darin:
  - Zuerst ein `<sc-if value="{{ wpNotLoaded }}" hint-placeholder-val="{{ true }}">` mit der Platzhalter-Karte:
    * Karten-Container passend zur hellen Sektion, z. B. `border:1px solid #E0DDD8;background:#FAF8F5;padding:clamp(28px,4vw,44px)`.
    * Hinweistext (Fließtext-Stil der Sektion, `font-size:16px;line-height:1.75;color:#6B6864`), sinngemäß: „Mit dem Laden des Wärmepumpen-Checks stimmen Sie zu, dass Daten – insbesondere Ihre IP-Adresse – an heizreport.de übertragen werden. Näheres dazu in unserer <a href="datenschutz/">Datenschutzerklärung</a>." Der Link nutzt exakt `href="datenschutz/"` (identisch zum Footer-Link der Startseite) und den Akzent-Linkstil der Seite.
    * `<button type="button" onclick="{{ loadWpCheck }}" …>` mit dem Primär-Button-Stil aus `<interfaces>` (Akzentfarbe, Uppercase, `style-hover="background:#A87800"`, `cursor:pointer`), Beschriftung „Wärmepumpen-Check laden".
    * Darunter eine kleine Fußnote (`font-size:13px;color:#8a8a8a`), sinngemäß: „Ihre Einwilligung gilt nur für diesen Seitenaufruf und wird nicht gespeichert." — deckt die Nicht-Speicherung transparent ab.
  - Danach der Widget-Wrapper: ein neuer `<div style="display: {{ wpDisplay }}">`, der die **unveränderte** Zeile `<div id="wp_check_script" class="heizreport" data-heizreport-ansprache="du" data-primary-color="#3371b7" data-heizreport-user="info@ms-bauconsult.de"></div>` umschließt.

WICHTIG — warum der Widget-Div NICHT in ein `sc-if` gehört: Der Renderer erzeugt das Widget-Div sonst beim Consent-Wechsel neu bzw. patcht ein wiederverwendetes Geschwister-Element, wodurch vom Drittanbieter-Script injizierte Kindknoten verloren gehen können. Der Div bleibt deshalb dauerhaft im DOM und wird ausschließlich über die interpolierte `display`-Eigenschaft des **umgebenden** Wrappers ein- und ausgeblendet. Am Snippet-Div selbst wird kein einziges Attribut interpoliert.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(grep -c 'setTimeout(_hrLoad,0)' index.html)" = "0" && test "$(grep -c 'this._hrLoad' index.html)" -ge 2 && test "$(grep -c 'loadWpCheck' index.html)" -ge 2 && test "$(grep -c 'wpConsent' index.html)" -ge 3 && test "$(grep -c 'wpNotLoaded' index.html)" = "2" && test "$(grep -c 'wpDisplay' index.html)" = "2" && test "$(grep -c 'id="wp_check_script" class="heizreport" data-heizreport-ansprache="du" data-primary-color="#3371b7" data-heizreport-user="info@ms-bauconsult.de"' index.html)" = "1" && test "$(grep -c 'heizreport.de/js/heizreport_3.js' index.html)" = "1" && test "$(grep -c 'href="datenschutz/"' index.html)" = "2" && node -e "const h=require('fs').readFileSync('index.html','utf8');const o=(h.match(/<sc-if[ >]/g)||[]).length,c=(h.match(/<\/sc-if>/g)||[]).length;if(o!==c)throw new Error('sc-if unbalanced '+o+'/'+c);console.log('sc-if balanced:',o)" && echo "TASK1 OK"</automated>
  </verify>
  <done>`heizreport_3.js` wird beim Seitenaufruf nicht mehr injiziert; die Sektion zeigt eine Platzhalter-Karte mit Hinweistext, Datenschutz-Link und Button; der Klick setzt `wpConsent` und injiziert das Script; das Kundensnippet ist zeichengleich erhalten; `sc-if`-Tags sind ausbalanciert.</done>
</task>

<task type="auto">
  <name>Task 2: Datenschutzerklärung auf Einwilligung umstellen, Containerbreiten vereinheitlichen, Sitemap bereinigen</name>
  <files>datenschutz/index.html, impressum/index.html, sitemap.xml</files>
  <action>
**2a — Heizreport-Abschnitt in `datenschutz/index.html` (Section ab Zeile 414) an die neue Rechtslage anpassen (locked decision 2).** Überschrift (Zeile 415) und Einleitungsabsatz (Zeile 416) bleiben. Zu ändern:
  - Absatz Zeile 417 (aktuell „Beim Aufruf unserer Startseite wird hierfür ein Script von den Servern des Anbieters geladen…"): umformulieren auf die Zwei-Klick-Lösung — das Widget wird **nicht** automatisch geladen; zunächst erscheint nur ein Hinweis mit Schaltfläche; erst nach aktivem Klick wird das Script von den Servern des Anbieters nachgeladen, wobei technisch zwingend die IP-Adresse übertragen wird und zusätzlich Browser- und Geräteinformationen sowie die aufrufende Seite übermittelt werden können.
  - Absatz Zeile 419 (Rechtsgrundlagen): Primäre Rechtsgrundlage ist jetzt die Einwilligung nach **Art. 6 Abs. 1 lit. a) DSGVO**, erteilt durch den Klick auf die Schaltfläche; der bisherige Verweis auf Art. 6 Abs. 1 lit. f) DSGVO für das Laden des Widgets entfällt. Der Zusatz zu Art. 6 Abs. 1 lit. b) DSGVO (Vertragsanbahnung) für die anschließend im Check gemachten Angaben bleibt erhalten. Ergänzend: Das Speichern von bzw. der Zugriff auf Informationen im Endgerät erfolgt gemäß § 25 Abs. 1 TDDDG ebenfalls erst nach dieser Einwilligung.
  - Neuer Absatz zum Widerruf und zur Nicht-Speicherung, vor dem Anbieter-Link (Zeile 420) einfügen: Die Einwilligung wird nicht gespeichert und gilt nur für den jeweiligen Seitenaufruf; beim nächsten Besuch bzw. nach einem Neuladen der Seite ist das Widget wieder deaktiviert, bis erneut zugestimmt wird — ein Widerruf für die Zukunft ist damit jederzeit ohne weiteres Zutun möglich.
  - Der Link auf `https://heizreport.de/datenschutz` (Zeile 420) bleibt unverändert.
  - Formatierung strikt am Bestand orientieren: normale Absätze `style="margin:0 0 14px;font-size:16px;line-height:1.85;color:#a8a8a8"`, hervorgehobene Rechtsgrundlagen-Absätze `color:#c2c2c2;font-weight:500`, letzter Absatz der Section `margin:0`.

**2b — Containerbreiten vereinheitlichen (locked decision 4a).** Aktueller Stand: `datenschutz/index.html` nutzt an allen 4 Stellen `max-width:860px`, `impressum/index.html` an allen 4 Stellen `max-width:980px` — die Inkonsistenz besteht also zwischen den Seiten. Vereinheitlichung auf **860px**: in `impressum/index.html` alle 4 Vorkommen `max-width:980px` (Zeilen 347, 357, 368, 402 — Header, Intro, Inhalt, Footer) auf `max-width:860px` ändern. Begründung: Der schmalere Satzspiegel ist für die textlastigen Rechtsseiten die bessere Lesebreite, und `datenschutz/index.html` (das längere Dokument) ist damit bereits konsistent — es bleibt unangetastet. Die `max-width:100%`-Regel in beiden Dateien nicht anfassen.

**2c — Sitemap/noindex-Widerspruch auflösen (locked decision 4c).** In `sitemap.xml` die beiden `<url>`-Blöcke für `https://ms-bauconsult.de/impressum/` und `https://ms-bauconsult.de/datenschutz/` vollständig entfernen (beide Seiten setzen `robots: noindex, follow`). Übrig bleibt genau der Eintrag für `https://ms-bauconsult.de/`. Die `robots`-Meta-Tags der beiden Seiten bleiben unverändert.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(grep -c 'Art. 6 Abs. 1 lit. a)' datenschutz/index.html)" -ge 1 && test "$(grep -c 'TDDDG' datenschutz/index.html)" -ge 1 && test "$(grep -c 'heizreport.de/datenschutz' datenschutz/index.html)" = "1" && test "$(grep -c 'max-width:980px' impressum/index.html)" = "0" && test "$(grep -c 'max-width:860px' impressum/index.html)" = "4" && test "$(grep -c 'max-width:860px' datenschutz/index.html)" = "4" && test "$(grep -c '<loc>' sitemap.xml)" = "1" && test "$(grep -c 'ms-bauconsult.de/</loc>' sitemap.xml)" = "1" && test "$(grep -c 'noindex' impressum/index.html)" -ge 1 && test "$(grep -c 'noindex' datenschutz/index.html)" -ge 1 && node -e "for(const f of ['datenschutz/index.html','impressum/index.html']){const h=require('fs').readFileSync(f,'utf8');const o=(h.match(/<div[ >]/g)||[]).length,c=(h.match(/<\/div>/g)||[]).length;if(o!==c)throw new Error(f+' div unbalanced '+o+'/'+c);console.log(f,'div balanced:',o)}" && node -e "const s=require('fs').readFileSync('sitemap.xml','utf8');if(!/^<\?xml/.test(s.trim()))throw new Error('kein XML-Prolog');const o=(s.match(/<url>/g)||[]).length,c=(s.match(/<\/url>/g)||[]).length;if(o!==c||o!==1)throw new Error('url-Tags: '+o+'/'+c);console.log('sitemap ok')" && echo "TASK2 OK"</automated>
  </verify>
  <done>Die Datenschutzerklärung nennt Art. 6 Abs. 1 lit. a) DSGVO als Rechtsgrundlage für das Widget, beschreibt das Laden erst nach Klick und die nicht gespeicherte, jederzeit hinfällige Einwilligung; Impressum und Datenschutz nutzen beide 860px; `sitemap.xml` listet nur noch die Startseite und bleibt wohlgeformt.</done>
</task>

<task type="auto">
  <name>Task 3: Intrinsische Bildmaße (width/height) an allen img-Tags ergänzen</name>
  <files>index.html, impressum/index.html, datenschutz/index.html</files>
  <action>
Locked decision 4b: Jedes `<img>` bekommt `width`- und `height`-Attribute mit den **echten Pixelmaßen der Datei** (Werte siehe `<interfaces>`, per jimp ausgelesen). Bestehende CSS-Größen in den `style`-Attributen bleiben unverändert — die CSS-Angaben gewinnen weiterhin, die Attribute liefern dem Browser nur das Seitenverhältnis zur CLS-Reduktion. Die Attribute jeweils direkt hinter `alt` einfügen, vor `style`.

**index.html:**
  - Zeile 1169 — `assets/logo.png` → `width="1281" height="413"`
  - Zeile 1198 — `assets/logo.png` → `width="1281" height="413"`
  - Zeile 1219 — `assets/shutterstock_34071343.png` (Hero) → `width="500" height="375"`
  - Zeile 1258 — `assets/ms.profil-300x246.png` → `width="300" height="246"`
  - Zeile 1263 — `assets/eee-300x73.png` → `width="300" height="73"`
  - Zeile 1422 — `assets/logo.png` (Footer) → `width="1281" height="413"`
  - Zeile 1327 — das getemplatete Leistungs-Bild: `width="{{ item.w }}" height="{{ item.h }}"` ergänzen und im `services`-Array in `renderVals()` (Zeilen 1537–1542) jedem Objekt `w`/`h` hinzufügen: `shutterstock_34071343.png` 500/375, `shutterstock_61563004.png` 1024/768, `kfw_bafa.png` 1015/903, `energieausweis-1.jpg` 250/354, `iSFP-sanierung-einzelmassnahmen-bafa-dena.jpg` 560/420, `unsplash_01.png` 500/400. Attribut-Interpolation ist an dieser Stelle nachweislich unterstützt (`src` und `alt` werden dort bereits interpoliert). Falls die visuelle Prüfung literale `{{ item.w }}`-Werte im gerenderten HTML zeigt, nur diesen einen `<img>`-Fall zurücknehmen und in der SUMMARY dokumentieren; alle anderen Bilder bleiben in jedem Fall bestückt.

**impressum/index.html:** Zeile 349 — `../assets/logo.png` → `width="1281" height="413"`
**datenschutz/index.html:** Zeile 348 — `../assets/logo.png` → `width="1281" height="413"`

Keine `loading`-Attribute ändern, keine Bilddateien anfassen, keine `style`-Werte anpassen.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "
const fs=require('fs');
let fail=0;
for(const f of ['index.html','impressum/index.html','datenschutz/index.html']){
  const imgs=(fs.readFileSync(f,'utf8').match(/<img\b[^>]*>/g)||[]);
  for(const t of imgs){
    if(!/\bwidth=/.test(t)||!/\bheight=/.test(t)){console.error('FEHLT in '+f+': '+t.slice(0,90));fail++;}
  }
  console.log(f, imgs.length+' img-Tags geprueft');
}
if(fail)throw new Error(fail+' img-Tag(s) ohne width/height');
const h=fs.readFileSync('index.html','utf8');
const svc=(h.match(/w:\s*\d+,\s*h:\s*\d+/g)||[]).length;
if(svc!==6)throw new Error('services w/h: '+svc+' statt 6');
console.log('services w/h ok');
" && echo "TASK3 OK"</automated>
  </verify>
  <done>Alle `<img>`-Tags in den drei Seiten tragen `width`/`height` mit den echten Dateimaßen; das `services`-Array enthält für alle 6 Einträge `w`/`h`; keine CSS-Größe wurde verändert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → heizreport.de | Drittanbieter-Script; überträgt IP-Adresse und Geräteinformationen des Besuchers |
| Browser → injiziertes `<script>` | Fremdcode läuft mit vollen Rechten im Origin ms-bauconsult.de |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-u8d-01 | Information Disclosure | Auto-Load von `heizreport_3.js` | mitigate | Kern dieses Plans: Script wird erst nach explizitem Klick injiziert (`loadWpCheck`), keine Übertragung vor Einwilligung |
| T-u8d-02 | Repudiation | Nachweis der Einwilligung | accept | Bewusste Entscheidung: keine Speicherung (kein Cookie/Storage). Einwilligung gilt pro Seitenaufruf, Nachweisrisiko minimal, da ohne Klick kein Request stattfindet |
| T-u8d-03 | Tampering | Fremdcode von heizreport.de im eigenen Origin | accept | Vom Kunden gewählter Anbieter, Snippet ist vorgegeben und darf nicht verändert werden; Einbindung nur nach Nutzeraktion begrenzt die Exposition |
| T-u8d-SC | Tampering | Paketinstallationen | n/a | Keine neuen Abhängigkeiten; `jimp` ist bereits als devDependency vorhanden und wird nur lesend für Bildmaße genutzt |
</threat_model>

<verification>
Automatisierte Gates: die drei Verify-Blöcke der Tasks (Grep-Zähler auf konkrete Code-Token plus Node-Prüfungen auf Tag-Balance und XML-Wohlgeformtheit).

<human-check>
Nur im Browser prüfbar, da die Seiten clientseitig gerendert werden und das Drittanbieter-Script offline nicht testbar ist:

1. `index.html` lokal öffnen (z. B. `python -m http.server` im Projektverzeichnis), DevTools → Netzwerk-Tab öffnen, Seite neu laden.
2. Zu „Wärmepumpe" scrollen: Es erscheint die Platzhalter-Karte mit Hinweistext, Link „Datenschutzerklärung" und Button „Wärmepumpen-Check laden". **Im Netzwerk-Tab darf kein Request an `heizreport.de` erscheinen.**
3. Button klicken: Platzhalter verschwindet, Request an `heizreport.de/js/heizreport_3.js` erscheint, Widget rendert an derselben Stelle.
4. Nach dem Laden ein paar Mal auf und ab scrollen (Scroll-Spy löst `setState` aus) — das Widget darf nicht verschwinden oder zurückgesetzt werden.
5. Seite neu laden: Platzhalter ist wieder da (keine gespeicherte Einwilligung).
6. Leistungs-Kacheln prüfen: Bilder rendern normal, keine literalen `{{ item.w }}`-Werte im DOM (DevTools → Elements).
7. `impressum/` und `datenschutz/` öffnen: beide Seiten gleich breit, Layout intakt, farbiges Logo unverändert.
</human-check>
</verification>

<success_criteria>
- Kein Request an heizreport.de vor einem Klick des Besuchers
- Platzhalter mit Sie-Ansprache, Datenschutz-Link (`href="datenschutz/"`) und Ladebutton in `#waermepumpencheck`
- Klick lädt Script und zeigt Widget; Kundensnippet zeichengleich unverändert
- Keine Consent-Speicherung (kein Cookie/localStorage/sessionStorage im Code)
- Datenschutzerklärung nennt Art. 6 Abs. 1 lit. a) DSGVO und § 25 Abs. 1 TDDDG, beschreibt Laden erst nach Klick und die nicht gespeicherte Einwilligung
- `impressum/index.html` und `datenschutz/index.html` durchgängig `max-width:860px`
- `sitemap.xml` enthält genau eine URL (Startseite) und ist wohlgeformt
- Alle `<img>`-Tags in den drei Seiten haben `width`/`height` mit echten Dateimaßen
- `sendMail.php`, das Kontaktformular und alle `index_bundled.html` sind unberührt (`git diff --stat` zeigt nur `index.html`, `datenschutz/index.html`, `impressum/index.html`, `sitemap.xml`)
</success_criteria>

<output>
Create `.planning/quick/260801-u8d-heizreport-widget-hinter-zwei-klick-cons/260801-u8d-SUMMARY.md` when done
</output>
